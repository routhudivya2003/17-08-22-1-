#include<stdio.h>
int main()
{
	int arr[50],i,n,sum=0,total;
	printf(" how many numbers \n");
	scanf("%d",&n);
	printf("\n enter the number ");
	for(i=0;i<n;i++)
	{
		scanf("%d",&arr[i]);
		sum=sum+arr[i];
	}
	n++;
	total=(n*(n+1))/2;
	printf("\n your missing number is %d",total-sum);
	return 0;
}
